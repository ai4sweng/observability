# Minimal dependency set for a KIO that pushes OTLP telemetry (the 7 mandatory
# metrics + heartbeat + traces + logs). These three are all the base demo needs.
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-grpc==1.44.0

# Optional: uncomment ONLY if you enable the Langfuse stream in .env
# (LLM prompt/completion/cost — see "Optional: Langfuse" in README.md).
# langfuse>=4.0.0,<5.0.0
