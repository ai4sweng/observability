# Pinned together: langfuse>=4 is itself built on OpenTelemetry and pulls in
# opentelemetry-sdk~=1.44 transitively. Keep these three in lockstep with it
# (verified via a clean-venv resolve) or pip's resolver will silently
# downgrade langfuse to the old, non-OTel v2.x line instead of erroring.
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-grpc==1.44.0
langfuse>=4.0.0,<5.0.0
